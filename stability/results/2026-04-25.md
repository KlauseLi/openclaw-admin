# Stability Results - 2026-04-25

## Context

This file starts the stability/pressure testing stage after the completed baseline was recorded in the main README and operation guide.

Known completed baseline:

- Direct OpenClaw chat to `claude-code` works.
- `run.sh async/watch/result --raw` works.
- Produced files are owned by `claude:claude`.
- Medium multi-file smoke task passed.
- Real repository low-risk script creation passed.

## ST-00 Baseline

Status: passed

Checks run:

```bash
bash scripts/check-claude-skill-state.sh
su - claude -c 'cd /home/claude/workspaces/openclaw-agent-smoke && ./scripts/validate_smoke.sh'
```

Observed:

```text
claude-skill-state-ok
validate-smoke-ok
```

Conclusion:

The known-good baseline is intact at the start of the stability stage.
